# Copy to .env.production before running `npm run build` for deployment.
# Point this at your EC2 instance's public URL (ideally with HTTPS via a load balancer or reverse proxy).
VITE_API_BASE=http://your-ec2-public-ip:8000
